import json

# ============================================
# CREATE UPDATED BLACK SPOTS JSON
# ============================================

# Project-defined black spot:
# Location having 5 or more accident records
black_spots = (
    location_accidents
    .filter("accident_count >= 5")
    .orderBy("accident_count", ascending=False)
)

# Collect all 527 black spots
black_spot_rows = black_spots.collect()

# Convert Spark rows to JSON format
black_spots_json = []

for row in black_spot_rows:
    black_spots_json.append({
        "latitude": float(row["lat_zone"]),
        "longitude": float(row["lon_zone"]),
        "accident_records": int(row["accident_count"])
    })

# Save JSON file
output_file = "/content/black_spots.json"

with open(output_file, "w") as f:
    json.dump(black_spots_json, f, indent=2)

# Verification
print("============================================")
print("BLACK SPOT JSON CREATED")
print("============================================")
print("Total black spots:", len(black_spots_json))
print("Output file:", output_file)

print("\nFirst 5 black spots:")
for spot in black_spots_json[:5]:
    print(spot)
